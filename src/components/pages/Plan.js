import React from 'react';
import '../../App.css';
import NavigationBar from '../NavigationBar';
import PlanSection from '../PlanSection';

const Plan = (props) => {
    return (
        <div>
            <NavigationBar />
            <PlanSection />
        </div>
    );
};

export default Plan;
